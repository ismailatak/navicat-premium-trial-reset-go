name: Release

on:
  push:
    tags:
      - 'v*.*.*'
    branches:
      - 'gh_actions'

jobs:
  release:
    strategy:
      matrix:
        os: [macos-latest]
        go-version: [1.23.x]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Set the GITHUB_TOKEN environment variable for the Go code
        run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV

      - name: Install Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          check-latest: true
          cache: false

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup local taps
        run: |
          mkdir -p taps
          curl -o taps/navicat-premium.rb https://raw.githubusercontent.com/Homebrew/homebrew-cask/HEAD/Casks/n/navicat-premium.rb
          sed -i '' '/zap trash/,/]/d' taps/navicat-premium.rb
          sed -i '' '/^$/d' taps/navicat-premium.rb

      - name: Install Navicat Premium
        run: |
          brew install --cask $(pwd)/taps/navicat-premium.rb

      - name: First Control - All list of ~/Library/Preferences
        run: sudo ls -la ~/Library/Preferences

      - name: Second Control - All list of ~/Library/Application Support
        run: sudo ls -la ~/Library/Application\ Support

      - name: Third Control - All list of /Applications
        run: sudo ls -la /Applications

      - name: Open Navicat Premium
        run: open -a /Applications/Navicat\ Premium.app

      - name: Fourth Control - All list of ~/Library/Preferences
        run: sudo ls -la ~/Library/Preferences

      - name: Fifth Control - All list of ~/Library/Application Support
        run: sudo ls -la ~/Library/Application\ Support

      - name: Sixth Control - All list of /Applications
        run: sudo ls -la /Applications

      - name: Is Navicat Premium running?
        run: |
          pgrep -fl Navicat
          ps aux | grep Navicat

      - name: Test
        run: |
          go run *.go